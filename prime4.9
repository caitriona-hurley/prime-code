prime = []

def list_builder(r):
    for y in range(2,r+1):
        for x in range(2,int(y ** 0.5)+1):
            if(y % x == 0):
                break
        else:
            prime.append(y)
                 
    print(prime[10000])

list_builder(110000)
